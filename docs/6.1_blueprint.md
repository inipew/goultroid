Native Go Telegram Userbot Framework

Revised Architecture & Technical Blueprint

Status: Architecture / Technical Design
Target: Native Go Telegram Userbot Framework
Protocol: Telegram MTProto
Approach: Architecture-first, runtime-first, plugin-last
Primary Goal: Robust, controllable, extensible, observable, and resource-safe runtime

---

1. Executive Summary

Framework ini dirancang sebagai native Go Telegram Userbot Runtime yang menggunakan Telegram MTProto sebagai transport dan protocol boundary.

Framework bukan sekadar kumpulan command Telegram.

Framework harus menyediakan runtime yang mampu mengelola:

- application lifecycle
- Telegram connection
- MTProto updates
- event bus
- command system
- queue
- worker
- scheduler
- storage
- services
- permissions
- plugin runtime
- feature lifecycle
- capability system
- resource ownership
- cleanup
- observability
- failure isolation
- rate limiting
- flood-wait handling
- idempotency
- state management

Tujuan utamanya adalah membuat framework yang:

- stabil
- predictable
- mudah dikembangkan
- mudah diuji
- mudah di-debug
- resource-safe
- tidak mudah mengalami goroutine leak
- tidak mengalami uncontrolled concurrency
- tidak memiliki plugin yang dapat merusak runtime global
- mampu melakukan graceful shutdown
- mampu melakukan plugin enable/disable secara aman
- mampu menangani workload ringan dan berat secara berbeda

Arsitektur utama:

UI / CLI
   │
   ▼
Application
   │
   ▼
Plugin Runtime
   │
   ▼
Features
   │
   ▼
Services
   │
   ▼
Runtime Infrastructure
   │
   ▼
Telegram Boundary
   │
   ▼
MTProto

Feature seperti:

AFK
PM Permit
Admin
Notes
Filters
Welcome
AntiSpam
Auto Reply
Downloader
Reminder

bukan bagian dari core runtime.

Feature tersebut berjalan melalui plugin.

---

2. Design Philosophy

2.1 Runtime First

Framework tidak dibangun dengan urutan:

buat command
buat AFK
buat PM Permit
buat Admin
baru memikirkan runtime

Tetapi:

Architecture
    ↓
Runtime
    ↓
Lifecycle
    ↓
Context
    ↓
Event
    ↓
Queue
    ↓
Worker
    ↓
Scheduler
    ↓
Storage
    ↓
Telegram Boundary
    ↓
Services
    ↓
Plugin Runtime
    ↓
Features

Feature baru dibuat setelah runtime cukup stabil.

---

2.2 Infrastructure First

Infrastructure harus memiliki lifecycle yang jelas.

Contoh:

Database
Scheduler
Worker Manager
EventBus
Telegram Client
Queue

tidak boleh dibuat secara ad-hoc oleh feature.

---

2.3 Plugins Consume Capabilities

Plugin tidak memiliki runtime.

Plugin menggunakan runtime.

Runtime
 ├── EventBus
 ├── Storage
 ├── Scheduler
 ├── Worker
 ├── Telegram
 ├── Services
 └── Diagnostics

Plugin
 └── consume capabilities

Prinsip:

«Plugins consume capabilities; they do not own the runtime.»

---

2.4 Every Long-Lived Resource Has an Owner

Setiap resource yang hidup lebih lama dari satu function invocation harus memiliki:

Owner
Cancellation Path
Cleanup Path
Lifecycle
Diagnostics

Contoh:

Event Subscription → plugin:afk
Scheduler Job      → plugin:reminder
Worker Task        → plugin:downloader
Temp File          → plugin:media
Managed Goroutine  → plugin:watcher

Tidak boleh ada resource anonim yang tidak diketahui siapa pemiliknya.

---

3. Layer Architecture

Framework dibagi menjadi beberapa boundary.

┌─────────────────────────────────────┐
│ UI / CLI                            │
└──────────────────┬──────────────────┘
                   │
┌──────────────────▼──────────────────┐
│ Application Layer                   │
│ Use Cases / Policies                │
└──────────────────┬──────────────────┘
                   │
┌──────────────────▼──────────────────┐
│ Plugin Runtime                      │
│ Lifecycle / Scope / Capability      │
└──────────────────┬──────────────────┘
                   │
┌──────────────────▼──────────────────┐
│ Feature / Plugin                    │
│ AFK / Admin / PM Permit / etc.      │
└──────────────────┬──────────────────┘
                   │
┌──────────────────▼──────────────────┐
│ Services                            │
│ Message / Permission / Media / etc. │
└──────────────────┬──────────────────┘
                   │
┌──────────────────▼──────────────────┐
│ Runtime Infrastructure              │
│ Event / Queue / Worker / Scheduler  │
│ Storage / Diagnostics / Config      │
└──────────────────┬──────────────────┘
                   │
┌──────────────────▼──────────────────┐
│ Telegram Boundary                   │
│ MTProto / Updates / RPC / Files     │
└─────────────────────────────────────┘

---

4. Core Architectural Rule

Gunakan aturan berikut:

Runtime
    = bagaimana sistem berjalan

Service
    = capability yang dapat digunakan banyak feature

Application
    = use case dan policy

Feature
    = business behavior

Plugin
    = packaging + lifecycle boundary untuk feature

Telegram Boundary
    = komunikasi dengan Telegram

Contoh:

AFK
 ├── Feature
 └── Plugin

Storage
 └── Runtime Capability

MessageService
 └── Reusable Service

Telegram Client
 └── Infrastructure

MTProto
 └── External Protocol

---

5. Dependency Direction

Dependency harus mengarah ke dalam.

UI
 ↓
Application
 ↓
Plugin Runtime
 ↓
Feature
 ↓
Service
 ↓
Infrastructure
 ↓
Telegram Boundary

Feature tidak boleh langsung bergantung pada implementation detail.

Contoh yang tidak diinginkan:

plugin
    ↓
*sql.DB

atau:

plugin
    ↓
*telegram.Client

atau:

plugin
    ↓
go cron.New()

Yang diinginkan:

plugin
    ↓
ctx.Storage()
ctx.Scheduler()
ctx.Workers()
ctx.Telegram()

---

6. Repository Structure

Struktur awal:

cmd/
    userbot/
        main.go

internal/
    application/

    runtime/
    lifecycle/
    events/
    queue/
    workers/
    scheduler/
    registry/

    services/

    plugins/

    telegram/

    storage/

    config/
    logging/
    diagnostics/
    ui/

pkg/
    telegram/

plugins/

    afk/
    pmpermit/
    admin/
    notes/
    filters/
    welcome/
    antispam/
    autoreply/
    downloader/
    reminder/

migrations/

configs/

docs/
    architecture/
    adr/

tests/

Feature berada di:

plugins/

sedangkan reusable capability berada di:

internal/services/

---

7. "cmd/"

"cmd/" hanya menjadi entry point.

Contoh:

cmd/userbot/main.go

Tanggung jawab:

load config
    ↓
create application
    ↓
start runtime
    ↓
wait signal
    ↓
shutdown

"main.go" tidak boleh berisi:

Telegram handler
Database logic
AFK logic
Scheduler logic
Worker logic
Plugin logic

---

8. Runtime

Runtime adalah pusat lifecycle sistem.

Runtime memiliki:

Context
Config
Logger
Diagnostics

Telegram
EventBus
Queue
Worker Manager
Scheduler
Storage

Service Registry
Plugin Runtime

Runtime bertanggung jawab atas:

- startup
- shutdown
- lifecycle
- dependency initialization
- resource ownership
- cancellation
- health
- failure handling

Runtime bukan tempat business logic feature.

---

9. Runtime State Machine

Runtime:

Created
   ↓
Initializing
   ↓
Starting
   ↓
Running
   ↓
Stopping
   ↓
Stopped

Failure:

Initializing ──→ Failed
Starting     ──→ Failed
Running      ──→ Failed

State harus observable.

Contoh:

type RuntimeState string

const (
    StateCreated      RuntimeState = "created"
    StateInitializing RuntimeState = "initializing"
    StateStarting     RuntimeState = "starting"
    StateRunning      RuntimeState = "running"
    StateStopping     RuntimeState = "stopping"
    StateStopped      RuntimeState = "stopped"
    StateFailed       RuntimeState = "failed"
)

---

10. Startup Order

Startup harus deterministic.

Config
  ↓
Logger
  ↓
Diagnostics
  ↓
Storage
  ↓
Migrations
  ↓
Telegram
  ↓
EventBus
  ↓
Queue
  ↓
Worker Manager
  ↓
Scheduler
  ↓
Services
  ↓
Plugin Runtime
  ↓
Discover Plugins
  ↓
Validate Plugins
  ↓
Resolve Dependencies
  ↓
Initialize Plugins
  ↓
Telegram Update Receiver
  ↓
Workers
  ↓
Scheduler
  ↓
Plugins
  ↓
Running

Plugin tidak boleh start sebelum capability yang dibutuhkannya tersedia.

---

11. Shutdown Order

Shutdown harus membatasi intake terlebih dahulu.

Stop accepting new work
        ↓
Stop plugin intake
        ↓
Stop Scheduler
        ↓
Cancel / Drain Queue
        ↓
Stop Workers
        ↓
Stop Plugins
        ↓
Stop Telegram Update Receiver
        ↓
Stop Telegram
        ↓
Flush logs / state
        ↓
Close Storage
        ↓
Stopped

Tujuan:

«Jangan shutdown infrastructure sebelum consumer berhenti menggunakannya.»

---

12. Context Is Mandatory

Semua operation yang berpotensi hidup lama harus menerima:

context.Context

Contoh:

func (s *Service) Run(ctx context.Context) error

Task:

func(ctx context.Context) error

HTTP:

func(ctx context.Context, req Request) (...)

Telegram:

func(ctx context.Context, ...) (...)

Plugin:

ctx.Go(func(ctx context.Context) {
    ...
})

Hindari:

go func() {
    for {
        ...
    }
}()

tanpa lifecycle owner.

---

13. Resource Ownership

Runtime memiliki global resources.

Runtime owns:
    Database
    Telegram connection
    EventBus
    Global Scheduler
    Worker Manager
    Queue
    Logger

Plugin memiliki scoped resources.

Plugin owns:
    Commands
    Event subscriptions
    Jobs
    Tasks
    Temporary files
    Plugin state
    Plugin configuration

Plugin tidak memiliki:

Global DB connection
Global Telegram client
Global scheduler
Global worker manager
Global lifecycle

---

14. Plugin Runtime

Plugin Runtime adalah subsystem yang mengelola plugin.

Komponen:

Plugin Manager
Plugin Manifest
Plugin Loader
Plugin Validator
Dependency Resolver
Capability Manager
Plugin Context
Plugin Scope
Resource Manager
Plugin Configuration
Plugin Lifecycle
Failure Isolation
Plugin Diagnostics

Plugin Runtime menjawab:

Plugin ini valid?
Dependency tersedia?
Capability boleh?
Bagaimana start?
Apa resource miliknya?
Bagaimana stop?
Apakah ada leak?

---

15. Plugin Lifecycle

Lifecycle:

Discovered
    ↓
Loaded
    ↓
Validated
    ↓
Initialized
    ↓
Enabled
    ↓
Running
    ↓
Stopping
    ↓
Disabled
    ↓
Unloaded

Failure:

Loaded ─────→ Failed
Validated ──→ Failed
Initialized → Failed
Running ────→ Failed

Disable plugin harus menghasilkan:

no new plugin work
no active subscription
no active scheduler job
no unmanaged goroutine
no transient task

Persistent data tetap boleh ada.

---

16. Plugin Manifest

Contoh:

type Manifest struct {
    ID           string
    Name         string
    Version      string
    Description  string

    Dependencies []Dependency
    Capabilities []Capability

    ConfigSchema Schema
}

Manifest menjadi deklarasi formal plugin.

Plugin tidak boleh diam-diam menggunakan capability yang tidak dideklarasikan.

---

17. Plugin Capability

Capability adalah hak plugin untuk menggunakan runtime resource.

Contoh:

telegram.read
telegram.send_message
telegram.edit_message
telegram.delete_message

storage.read
storage.write

scheduler
worker
events

http
cache

filesystem.temp
filesystem.data

media.download
media.process

telegram.raw

Capability harus eksplisit.

Contoh:

AFK:
    telegram.read
    telegram.send_message
    storage.read
    storage.write
    events

Reminder:
    storage.read
    storage.write
    scheduler
    telegram.send_message

"telegram.raw" sebaiknya menjadi capability privileged.

---

18. Capability vs User Permission

Keduanya berbeda.

Plugin Capability

Menentukan:

Apa yang plugin boleh gunakan dari runtime?

User Permission

Menentukan:

Siapa yang boleh melakukan action?

Contoh:

Plugin Admin
    capability:
        telegram.send_message
        telegram.delete_message
        storage
        events

User:
    Owner
    Sudo
    Admin
    Authorized
    Everyone

Model authorization:

Actor
  ↓
Permission
  ↓
Policy
  ↓
Decision
  ↓
Action

Jangan hard-code:

if userID == 123456789 {
    ...
}

Gunakan:

permission.Check(ctx, actor, permission.Owner)

---

19. Plugin Context

Plugin tidak menerima seluruh Runtime.

Plugin menerima scoped context.

Contoh API:

type Context interface {
    Context() context.Context

    Manifest() Manifest
    Logger() Logger
    Config() Config

    Events() EventBus
    Storage() Storage
    Scheduler() Scheduler
    Workers() WorkerManager
    Telegram() Telegram
    Services() ServiceRegistry

    Files() Filesystem

    Go(func(context.Context))
    Defer(func())
}

Dengan demikian:

Plugin
   ↓
Plugin Context
   ↓
Scoped Capabilities

bukan:

Plugin
   ↓
Global Runtime

---

20. Plugin Scope

Plugin Scope adalah boundary resource plugin.

Scope menyimpan:

Context
Cancellation
Event subscriptions
Scheduler jobs
Worker tasks
Managed goroutines
Cleanup callbacks
Temporary files
Metrics metadata

Ketika plugin dihentikan:

scope.Cancel()
     ↓
cancel tasks
     ↓
cancel jobs
     ↓
unsubscribe events
     ↓
stop goroutines
     ↓
cleanup
     ↓
wait
     ↓
verify

---

21. Structured Concurrency

Semua background work harus memiliki parent.

Contoh:

ctx.Go(func(ctx context.Context) {
    for {
        select {
        case <-ctx.Done():
            return
        default:
        }
    }
})

Konsep:

Runtime Context
    │
    ├── Plugin Context
    │      │
    │      ├── Event Handler
    │      ├── Scheduler Task
    │      ├── Worker Task
    │      └── Background Goroutine
    │
    └── Infrastructure

Jika plugin berhenti:

Plugin Context canceled
        ↓
semua child work berhenti

---

22. Cleanup Registry

Plugin dapat mendaftarkan cleanup:

ctx.Defer(func() {
    // cleanup
})

Cleanup harus:

- idempotent
- bounded
- cancellation-aware
- tidak bergantung pada resource yang sudah ditutup

Urutan cleanup harus predictable.

---

23. EventBus

EventBus menjadi communication layer.

Flow:

Telegram
   ↓
Update Receiver
   ↓
Normalizer
   ↓
Internal Event
   ↓
EventBus
   ↓
Subscribers

EventBus bukan database.

EventBus juga bukan universal queue.

EventBus bertanggung jawab untuk:

publish
subscribe
unsubscribe
dispatch
ordering
priority
error handling
panic recovery
backpressure policy

---

24. Managed Event Subscription

Subscription harus dimiliki plugin.

Contoh:

sub := ctx.Events().Subscribe(
    MessageReceived{},
    handler,
)

ctx.Defer(sub.Close)

Lebih baik:

ctx.Events().On(
    MessageReceived{},
    handler,
)

dan Plugin Scope otomatis memiliki subscription tersebut.

Ketika plugin stop:

Plugin Scope
    ↓
unsubscribe all

Tidak boleh ada subscription orphan.

---

25. Event Semantics

EventBus harus mendefinisikan:

Synchronous / Asynchronous
Ordering
Priority
Concurrency
Backpressure
Retry
Panic Recovery
Error Handling
Cancellation
Shutdown behavior

Event handler tidak boleh dieksekusi sambil memegang internal lock EventBus.

---

26. Event Types

Contoh event:

MessageReceived
MessageEdited
MessageDeleted

ChatJoined
ChatLeft

UserUpdated
ChatUpdated

CallbackQueryReceived
InlineQueryReceived

MediaReceived

CommandReceived

PluginStarted
PluginStopped

SchedulerJobTriggered
TaskStarted
TaskCompleted
TaskFailed

Pisahkan:

External Event
Internal Event
Domain Event
Lifecycle Event

---

27. Telegram Update Normalization

Telegram update tidak boleh langsung diberikan ke feature.

Flow:

MTProto Update
      ↓
Update Receiver
      ↓
Update Normalizer
      ↓
Internal Event
      ↓
EventBus

Normalizer bertugas menghasilkan model internal yang konsisten.

Contoh:

Raw Telegram Update
        ↓
MessageReceived
        ↓
Message
        ├── ID
        ├── Chat
        ├── Sender
        ├── Text
        ├── Entities
        ├── Reply
        ├── Media
        └── Timestamp

---

28. Queue

Queue menjadi buffer antara producer dan consumer.

Producer
   ↓
Queue
   ↓
Worker

Queue harus bounded.

Contoh:

capacity = 1000

Jangan default:

unlimited queue

karena Telegram event burst dapat menyebabkan memory growth.

Queue harus memiliki policy ketika penuh:

Block
Reject
Drop
Coalesce
Priority

Policy harus eksplisit berdasarkan workload.

---

29. Worker

Worker bertanggung jawab atas concurrency.

Model:

Queue
  ↓
Worker 1
Worker 2
Worker 3
Worker 4

Worker menentukan:

berapa task boleh berjalan bersamaan

Task menentukan:

apa yang dilakukan

Prinsip:

Scheduler = WHEN
Queue     = PRESSURE / BUFFER
Worker    = HOW MANY
Task      = WHAT

---

30. Jangan Satu Global Worker Pool

Tidak semua workload sama.

Jangan:

ALL TASKS
    ↓
Global Worker Pool

Lebih baik:

Event Workers
Job Workers
Download Workers
Media Workers
Task Workers

Contoh:

Download:
    concurrency = 3

Media:
    concurrency = 2

General Tasks:
    concurrency = 8

Hal ini mencegah workload berat menghabiskan seluruh concurrency runtime.

---

31. Plugin Worker Pool

Plugin mendapatkan managed worker capability.

Contoh:

ctx.Workers().Submit(ctx, Task{
    Name: "download",
    Run: func(ctx context.Context) error {
        return downloader.Download(ctx, url)
    },
})

Plugin tidak perlu membuat:

sync.WaitGroup
chan Task
go worker()

sendiri untuk workload standar.

Runtime yang mengontrol:

Concurrency
Queue
Timeout
Retry
Cancellation
Shutdown
Ownership
Metrics

---

32. Plugin Worker Quota

Plugin dapat memiliki quota.

Contoh:

max workers
max queued tasks
max running tasks
max background goroutines
max subscriptions
max scheduler jobs

Contoh:

plugin:downloader
    workers: 3
    queue: 50

plugin:reminder
    workers: 2
    queue: 100

Quota mencegah satu plugin menghabiskan resource runtime.

Quota dapat diberlakukan bertahap.

Versi awal cukup:

max concurrent tasks
max queued tasks

---

33. Scheduler

Scheduler menentukan kapan pekerjaan dijalankan.

Scheduler bukan worker.

Scheduler
    ↓
Trigger
    ↓
Queue
    ↓
Worker
    ↓
Task

Scheduler cocok untuk:

Reminder
AFK timeout
Permit expiration
Scheduled message
Cleanup
Periodic synchronization
Retry

Heavy work sebaiknya tidak dieksekusi langsung oleh scheduler.

---

34. Scheduler API

Contoh:

job, err := ctx.Scheduler().Every(
    "5m",
    func(ctx context.Context) error {
        return task.Run(ctx)
    },
)

Lebih baik jika scheduler menerima metadata:

Job{
    ID:       "...",
    Owner:    "plugin:reminder",
    Schedule: "...",
    Timeout:  "...",
    Retry:    RetryPolicy{},
    Run:      ...,
}

Job harus memiliki identity.

---

35. Scheduler Job Ownership

Setiap job memiliki owner.

job:123
owner: plugin:reminder

Plugin tidak boleh membuat global job yang tidak tercatat.

Ketika plugin disable:

plugin:reminder
       ↓
Scheduler
       ↓
cancel all owned jobs

Resource manager harus dapat menjawab:

Berapa job plugin ini?
Job apa saja?
Mana yang running?
Mana yang pending?

---

36. Persistent Scheduler

Scheduler dapat memiliki persistent jobs.

Contoh:

Reminder
Scheduled Message
Recurring Cleanup
Permit Expiration

Persistent job disimpan ke storage.

Runtime restart:

Database
   ↓
Load Jobs
   ↓
Validate
   ↓
Reconstruct Runtime Jobs

Jangan menyimpan function pointer.

Simpan declarative state:

job_id
plugin_id
type
schedule
payload
status
next_run
retry_policy

---

37. Scheduler Recovery

Scheduler harus memiliki recovery policy.

Jika runtime mati ketika job seharusnya berjalan:

RUN_IMMEDIATELY
SKIP
RECALCULATE

Policy bergantung pada jenis job.

Contoh reminder:

RUN_IMMEDIATELY

Contoh periodic cleanup:

RECALCULATE

Scheduler harus menghindari duplicate execution.

Gunakan:

Job ID
Execution ID
Idempotency Key

---

38. Storage

Storage adalah infrastructure global.

Runtime memiliki:

DB connection
DB pool
transactions
migrations
health
backup/recovery integration

Plugin tidak membuka database sendiri.

Plugin menggunakan:

ctx.Storage()

Storage mendukung:

KV
Repository
Transaction
Migration
Namespace

---

39. Storage Namespace

Setiap plugin memiliki namespace.

Contoh:

plugins.afk
plugins.pmpermit
plugins.admin
plugins.notes
plugins.reminder

Plugin:

plugin:afk

tidak boleh membaca:

plugin:admin

secara default.

Namespace mencegah accidental coupling.

---

40. Repository Abstraction

Feature tidak bergantung langsung pada SQL driver.

Contoh:

type AFKRepository interface {
    Get(ctx context.Context, userID int64) (*AFKState, error)
    Set(ctx context.Context, state AFKState) error
    Delete(ctx context.Context, userID int64) error
}

Implementasi:

PostgreSQL
SQLite
Mock

dapat diganti tanpa mengubah feature.

---

41. Database Ownership

Core memiliki database.

Plugin memiliki domain state.

Runtime
 └── Database

Plugin
 └── Repository
       └── Plugin-owned state

Plugin tidak boleh:

sql.Open(...)

atau:

db := globalDB

secara langsung.

---

42. Migration

Migration memiliki dua kategori:

Core Migration
Plugin Migration

Contoh:

migrations/
    core/
    plugins/
        afk/
        reminder/
        notes/

Plugin migration harus dapat dikaitkan dengan:

plugin ID
plugin version
migration version

Upgrade plugin:

Old State
   ↓
Migration
   ↓
New State

Persistent state dan runtime state harus dipisahkan.

---

43. Telegram Boundary

Telegram adalah external system.

Boundary:

Application
    ↓
Telegram Service
    ↓
Telegram Abstraction
    ↓
MTProto Adapter
    ↓
Telegram

Jangan membuat business logic langsung bergantung pada MTProto types jika tidak diperlukan.

---

44. Telegram Abstraction

Plugin sebaiknya menggunakan abstraction:

type Telegram interface {
    SendMessage(...)
    EditMessage(...)
    DeleteMessage(...)
    GetEntity(...)
    DownloadMedia(...)
}

Bukan:

plugin
   ↓
raw MTProto client

Raw access harus menjadi privileged capability.

---

45. MTProto

MTProto boundary menangani:

Authentication
Session
Connection
Reconnect
RPC
Update Receiver
Flood Wait
Entity Resolution
File Transfer

Gunakan library Go yang sesuai jika tujuan framework adalah membangun userbot application, bukan mengimplementasikan protocol MTProto dari nol.

Jika implementasi MTProto sendiri memang menjadi tujuan, MTProto harus diperlakukan sebagai subsystem tersendiri dengan boundary yang sangat ketat.

---

46. Flood Wait

Telegram dapat mengembalikan flood wait.

Flood wait tidak boleh ditangani oleh setiap plugin secara terpisah.

Flow:

Plugin
   ↓
Telegram Service
   ↓
Rate Limiter
   ↓
MTProto
   ↓
Flood Wait
   ↓
Flood Wait Manager

Flood wait harus menjadi centralized policy.

Contoh:

retry after 30s

harus diketahui runtime.

Plugin tidak seharusnya melakukan:

time.Sleep(30 * time.Second)

tanpa context.

---

47. Telegram Rate Limiting

Rate limiter berbeda dengan worker concurrency.

Worker
    = berapa task berjalan

Rate Limiter
    = seberapa cepat request dikirim

Flood Control
    = bagaimana merespons pembatasan Telegram

Rate limit dapat memiliki scope:

Global
Per method
Per chat
Per user
Per plugin

Contoh:

SendMessage
    ↓
Rate Limiter
    ↓
Telegram

Rate limiter harus context-aware dan shutdown-safe.

---

48. Media and File Transfer

Media adalah heavy workload.

Harus memiliki:

separate queue
concurrency limit
timeout
retry
progress
temporary storage
cleanup
cancellation

Flow:

Plugin
   ↓
MediaService
   ↓
Download Queue
   ↓
Download Worker
   ↓
Telegram File Transfer
   ↓
Temporary File
   ↓
Processing
   ↓
Cleanup

Jangan:

go downloadEverything()

tanpa ownership.

Temporary file harus memiliki owner:

plugin:downloader
task:download-123

dan cleanup harus tetap berjalan jika task gagal atau plugin dihentikan.

---

49. Feature Architecture

Feature adalah business/use-case concept.

Contoh:

AFK
PM Permit
Admin
Notes
Filters
Welcome
AntiSpam
Auto Reply
Downloader
Reminder

Feature bukan infrastructure.

Feature menggunakan:

Plugin Runtime
Services
Runtime Capabilities

---

50. Feature ≠ Plugin

Feature dan plugin harus dibedakan secara konsep.

Feature
    = apa yang userbot lakukan

Plugin
    = bagaimana feature dikemas dan dikelola runtime

Contoh:

AFK Feature
    ↓
AFK Plugin

Namun satu plugin dapat memiliki banyak feature yang sangat berkaitan.

Contoh:

Admin Plugin
 ├── .ban
 ├── .unban
 ├── .mute
 ├── .unmute
 ├── .kick
 └── .warn

Tidak perlu:

ban-plugin
unban-plugin
mute-plugin
kick-plugin

---

51. Feature Contract

Setiap feature harus memiliki contract.

Minimal:

Feature ID
Name
Description
Dependencies
Capabilities
Configuration
Persistent State
Commands
Events
Jobs
Tasks
Permissions
Failure Policy

Contoh:

AFK Feature

ID:
    afk

Dependencies:
    message-service
    permission-service

Capabilities:
    events
    storage
    telegram.send_message

State:
    afk status
    reason
    timestamp

Events:
    MessageReceived

Commands:
    .afk
    .unafk

Contract ini menjadi dasar validasi runtime.

---

52. Feature State Model

State dibagi menjadi tiga kategori.

Persistent State

Bertahan setelah restart.

Contoh:

AFK status
Reminder
Notes
Filters
Settings
Permissions

Runtime State

Hanya berlaku selama runtime hidup.

Contoh:

active subscriptions
running jobs
worker tasks
plugin context
cache handles

Ephemeral State

Bersifat sementara.

Contoh:

temporary files
temporary request state
in-flight progress
temporary buffers

Rule:

Persistent State survives restart.
Runtime State is reconstructed.
Ephemeral State is discarded.

---

53. Feature Dependencies

Feature dapat bergantung pada capability/service lain.

Contoh:

Reminder
    ↓
Storage
Scheduler
MessageService

atau:

Admin
    ↓
PermissionService
ModerationService
EntityService
MessageService

Dependency harus dideklarasikan.

Jangan hidden dependency.

---

54. Feature Conflicts

Feature dapat saling konflik.

Contoh:

AutoReply
    vs
PM Permit

atau:

Multiple command handlers

Runtime harus dapat mengetahui:

Feature A conflicts with Feature B

Conflict policy dapat berupa:

Reject
Priority
Exclusive
Chain
Override

---

55. Feature Interaction

Feature harus dapat berinteraksi melalui event/service contract, bukan global variable.

Contoh:

MessageReceived
    ↓
PM Permit
    ↓
PermitRequested
    ↓
AFK
    ↓
Message Service

Event ordering harus predictable.

Feature tidak boleh mengandalkan:

"plugin A biasanya jalan lebih dulu"

tanpa contract resmi.

---

56. Event Ordering

EventBus harus mendukung ordering policy.

Contoh:

Priority 100
    Permission / Security

Priority 50
    Normalization / Filtering

Priority 10
    Feature handlers

Ordering harus terdokumentasi.

Jika event membutuhkan strict ordering:

Event A
   ↓
Event B
   ↓
Event C

dependency tersebut harus explicit.

---

57. Event Priority

Priority hanya digunakan ketika memang dibutuhkan.

Contoh:

Security / Authorization
    high priority

Core normalization
    medium-high

Feature
    normal

Analytics
    low

Priority tidak boleh menjadi alasan untuk membuat feature saling bergantung secara implisit.

---

58. Event Middleware

Event pipeline dapat memiliki middleware.

Raw Event
   ↓
Middleware
   ↓
Handler

Middleware dapat menangani:

logging
authorization
deduplication
metrics
rate limiting
panic recovery
tracing

Contoh:

MessageReceived
   ↓
Dedup Middleware
   ↓
Permission Middleware
   ↓
Metrics Middleware
   ↓
Feature Handler

---

59. Event Hooks

Hook digunakan untuk lifecycle/event extension.

Contoh:

BeforeMessage
AfterMessage

BeforeCommand
AfterCommand

BeforeTask
AfterTask

BeforePluginStart
AfterPluginStart

Hook harus memiliki contract yang jelas.

Jangan menjadikan hook sebagai mekanisme global tanpa batas karena dapat menciptakan hidden control flow.

---

60. Command Architecture

Command system adalah subsystem tersendiri.

Flow:

MessageReceived
      ↓
Command Parser
      ↓
Command Registry
      ↓
Command Resolution
      ↓
Middleware
      ↓
Permission
      ↓
Handler
      ↓
Application
      ↓
Service
      ↓
Telegram

---

61. Command Registry

Command harus didaftarkan melalui registry.

Metadata minimal:

Name
Aliases
Description
Usage
Category
Owner Plugin
Priority
Permission
Cooldown
Flags

Contoh:

.afk
    owner: plugin:afk
    permission: everyone

---

62. Command Conflict Resolution

Dua plugin dapat mendaftarkan command yang sama.

Contoh:

.info

Runtime harus menentukan policy:

Reject duplicate
Priority
Namespace
Override
Alias resolution

Default sebaiknya:

duplicate command = registration error

agar behavior tidak ambigu.

---

63. Command Middleware

Command middleware dapat menangani:

Authentication
Authorization
Cooldown
Rate Limit
Logging
Metrics
Argument validation
Context enrichment

Contoh:

Command
   ↓
Auth
   ↓
Permission
   ↓
Cooldown
   ↓
Handler

---

64. Command Context

Command handler menerima context yang sudah dinormalisasi.

Contoh:

type CommandContext struct {
    Context context.Context

    Prefix string
    Name   string
    Args   []string
    Flags  map[string]string

    Sender Actor
    Chat   Chat
    Message Message
    Reply   *Message
}

Dengan ini feature tidak perlu parsing raw MTProto update berulang kali.

---

65. Permission Model

Permission harus menjadi service.

Contoh:

Owner
Sudo
Admin
Authorized
Everyone

Model:

Actor
   ↓
Permission Service
   ↓
Policy
   ↓
Decision

Feature:

if !permissions.Allowed(ctx, actor, PermissionAdmin) {
    return ErrPermissionDenied
}

---

66. Entity and Identity Model

Telegram entity harus dinormalisasi.

Model internal:

User
Chat
Channel
Message
Peer
Entity

Identity harus memiliki stable identifier.

Contoh:

UserID
ChatID
MessageID
PeerID

Jangan membandingkan entity berdasarkan pointer.

user1 == user2

tidak cukup.

Gunakan identity:

user.ID
chat.ID

---

67. Entity Resolver

Telegram sering memberikan entity yang tidak lengkap.

Karena itu diperlukan:

EntityResolver
EntityCache

Flow:

Raw Peer
   ↓
Entity Resolver
   ↓
Cache
   ↓
Normalized Entity

Service ini dipakai oleh:

Admin
Moderation
Notes
PM Permit
AFK
Filters

---

68. Request Deduplication

Telegram update atau retry dapat menyebabkan event diproses lebih dari satu kali.

Runtime harus mendukung deduplication.

Contoh identity:

Update ID
Message ID
Callback ID
Task ID
Job Execution ID

Deduplication policy harus eksplisit.

---

69. Idempotency

Task penting harus idempotent.

Contoh:

Send scheduled message
Apply moderation action
Process reminder
Download media
Update state

Gunakan:

Idempotency Key

Contoh:

reminder:123:execution:456

Jika task dieksekusi ulang:

same key
    ↓
already processed?
    ↓
skip

Hal ini penting untuk retry dan recovery.

---

70. Transaction Boundary

Transaction harus ditentukan pada application/use-case level.

Contoh:

Create Reminder

BEGIN
    save reminder
    save schedule state
COMMIT

Jangan membuat transaction boundary berdasarkan:

satu function SQL

jika business operation sebenarnya terdiri dari beberapa perubahan.

Transaction harus menjawab:

«Apa unit perubahan state yang harus atomic?»

---

71. Retry Model

Retry tidak boleh dilakukan secara acak oleh setiap plugin.

Retry policy:

MaxAttempts
Backoff
Jitter
RetryableErrors
Timeout

Contoh:

attempt 1
    ↓
1s
    ↓
attempt 2
    ↓
2s
    ↓
attempt 3

Jangan retry error yang memang permanent.

Contoh:

PermissionDenied
ValidationError
InvalidEntity

biasanya tidak perlu retry.

---

72. Error Model

Error harus memiliki kategori.

ConfigurationError
InitializationError
ValidationError
PermissionError

TelegramError
FloodWaitError

StorageError
SchedulerError
PluginError

TimeoutError
CancellationError

Error digunakan untuk menentukan:

Retry?
Abort?
Log?
Notify?
Ignore?
Disable plugin?

---

73. Plugin Failure Isolation

Panic plugin tidak boleh menjatuhkan runtime.

Boundary:

Plugin
   ↓
Recovery Boundary
   ↓
Runtime

Panic harus:

recover
log stack
attribute plugin
record metric
apply failure policy

Policy dapat:

continue
restart plugin
disable plugin

tergantung severity.

---

74. Resource Manager

Resource Manager mencatat resource yang dimiliki plugin.

Contoh:

subscriptions
jobs
tasks
goroutines
temporary files
queues
connections

Query:

plugin:afk

dapat menghasilkan:

subscriptions: 2
jobs: 1
tasks: 0
goroutines: 1
temp_files: 0

Ini penting untuk leak detection.

---

75. Leak Detection

Invariant:

Plugin stopped
    ↓
plugin-owned transient resources = 0

Jika tidak:

WARNING:
plugin:example has
1 active subscription
2 running tasks
1 goroutine

Development mode dapat melakukan strict assertion.

---

76. Service Layer

Service adalah reusable capability.

Contoh:

MessageService
PermissionService
ModerationService
EntityService
MediaService
DownloaderService
CacheService
HTTPService
TelegramService

Service bukan feature.

---

77. Application Layer

Application layer berisi use case.

Contoh:

CreateReminder
SetAFK
ClearAFK
RequestPMPermit
ApprovePMPermit
BanUser
MuteUser
DownloadMedia
CreateNote

Application layer menggabungkan:

Policy
Service
Storage
Transaction

Feature/plugin memanggil use case.

---

78. Example: AFK

AFK adalah feature.

AFK Plugin
    ↓
Message Event
    ↓
AFK Application Use Case
    ↓
AFK Repository
    ↓
Message Service
    ↓
Telegram

AFK dapat menggunakan:

EventBus
Storage
PermissionService
MessageService
Scheduler

Scheduler hanya diperlukan jika ada expiration/auto-clear behavior.

---

79. Example: PM Permit

PM Permit adalah feature.

Private Message
    ↓
PM Permit Plugin
    ↓
Permission / Policy
    ↓
Storage
    ↓
Scheduler
    ↓
Telegram

State:

pending
approved
denied
expired

Expiration sebaiknya menggunakan scheduler managed runtime.

---

80. Example: Admin

Admin sebaiknya menjadi feature group.

Admin Plugin
    ├── ban
    ├── unban
    ├── mute
    ├── unmute
    ├── kick
    ├── warn
    └── purge

Tetapi capability reusable berada di services:

PermissionService
EntityService
ModerationService
MessageService
ChatService

Dengan demikian:

Admin Plugin
AntiSpam Plugin
AutoModeration Plugin

dapat menggunakan:

ModerationService

tanpa menduplikasi logic Telegram moderation.

---

81. Configuration

Configuration harus centralized.

Contoh:

runtime:
  shutdown_timeout: 30s

telegram:
  session: ...

workers:
  general:
    concurrency: 8

scheduler:
  enabled: true

plugins:
  afk:
    enabled: true

  reminder:
    enabled: true

Plugin hanya membaca konfigurasi miliknya.

---

82. Plugin API Versioning

Plugin API harus memiliki version.

Contoh:

plugin-api/v1
plugin-api/v2

Manifest:

api_version: 1

Runtime dapat menolak plugin incompatible.

Hal ini penting jika framework berkembang tanpa merusak semua plugin.

---

83. Plugin Dependency Resolver

Dependency graph:

Admin
  ↓
PermissionService

Reminder
  ↓
Scheduler
  ↓
Storage

Plugin dependency harus diperiksa:

dependency exists?
version compatible?
cycle?
capability available?

Cycle:

A → B
B → A

harus ditolak.

Gunakan topological sort untuk initialization order.

---

84. Plugin Configuration Schema

Plugin configuration harus dapat divalidasi.

Contoh:

plugins:
  reminder:
    default_duration: 10m
    max_reminders: 100

Schema memvalidasi:

type
required fields
range
enum
default

Configuration error sebaiknya terjadi sebelum plugin masuk "Running".

---

85. Plugin Registration

Semua registration harus dimiliki plugin.

Plugin
 ├── Commands
 ├── Event handlers
 ├── Jobs
 ├── Services
 ├── Metrics
 └── Routes

Ketika plugin disabled:

unregister all owned registrations

Tidak boleh meninggalkan registration global.

---

86. Plugin Storage vs Runtime Resource

Ini adalah boundary penting.

Persistent:

AFK state
Reminder
Notes
Filters
Settings

Transient:

Subscription
Job handle
Worker task
Goroutine
Temp file

Ketika plugin disable:

Persistent state → tetap
Transient resource → cleanup

---

87. Runtime Invariants

Framework harus mempertahankan invariant berikut.

Plugin disabled
    → no new plugin work

Plugin stopped
    → no plugin-owned transient resource

Queue
    → never exceeds capacity

Worker
    → never exceeds concurrency

Scheduler
    → never bypasses worker policy

Plugin
    → cannot use ungranted capability

Task
    → has owner

Job
    → has owner

Subscription
    → has owner

Long-lived goroutine
    → has cancellation path

---

88. Observability

Runtime harus observable.

Minimal:

Logs
Metrics
Health
Diagnostics

Plugin metrics:

events received
events failed

commands executed
commands failed

tasks submitted
tasks running
tasks failed

jobs registered
jobs running

queue depth
execution duration
errors

Log metadata:

plugin_id
task_id
job_id
execution_id
request_id
chat_id
user_id

---

89. Correlation ID

Satu request dapat memiliki correlation ID.

Flow:

Telegram Update
      ↓
request_id
      ↓
Event
      ↓
Command
      ↓
Application
      ↓
Task
      ↓
Service
      ↓
Telegram Response

Hal ini memudahkan debugging:

Kenapa command .download gagal?

Runtime dapat mencari:

request_id

dan mengikuti seluruh execution chain.

---

90. Testing Architecture

Setiap subsystem harus dapat dites tanpa Telegram nyata.

Unit test:

Command Parser
Command Registry
EventBus
Queue
Worker
Scheduler
Permission
Storage
Plugin Scope
Resource Manager

Integration test:

Runtime
   ↓
EventBus
   ↓
Plugin
   ↓
Service
   ↓
Worker

Telegram integration test harus terpisah.

---

91. Test Plugin Context

Framework sebaiknya menyediakan:

NewTestPluginContext()

dengan fake:

Fake Telegram
Fake Storage
Fake Scheduler
Fake Worker
Fake EventBus
Fake HTTP
Fake Clock

Dengan demikian plugin dapat dites tanpa menjalankan seluruh runtime.

---

92. Deterministic Scheduler Testing

Scheduler harus menggunakan abstraction:

type Clock interface {
    Now() time.Time
}

Test dapat menggunakan fake clock.

Contoh:

Now = 10:00
Advance 5 minutes
Scheduler triggers job

Tanpa:

time.Sleep(5 * time.Minute)

---

93. Plugin Scope Test

Test penting:

Start plugin
↓
Register event
↓
Register job
↓
Start task
↓
Stop plugin
↓
Assert:
    subscriptions = 0
    jobs = 0
    tasks = 0
    goroutines = 0
    temp resources = 0

Ini menjadi regression test untuk memory/resource leak.

---

94. Race Detection

Development pipeline wajib mendukung:

go test -race ./...

Race-prone area:

EventBus
Plugin Scope
Scheduler
Worker Manager
Resource Manager
Cache
Runtime State
Plugin Enable/Disable

harus mendapat perhatian khusus.

---

95. Anti-Patterns

Hindari:

Clone Python architecture
Feature-first development
Unmanaged goroutines
Plugin-owned global scheduler
Plugin-owned global DB
Plugin-owned global worker pool
Raw MTProto everywhere
Hardcoded owner IDs
Unlimited queue
Unlimited concurrency
Global singleton
UI mutating internals
Plugin controlling runtime lifecycle
Hidden feature dependency
Hidden event ordering
Duplicate command without policy
Retry everything
time.Sleep without context
Temp file without cleanup

---

96. Core Definition of Done

Core belum dianggap selesai jika belum memiliki:

Config
Logger
Lifecycle
Context
Graceful Shutdown

Runtime State

EventBus
Queue
Backpressure

Worker Manager

Scheduler
Retry
Timeout

Storage
Migration

Diagnostics

---

97. Telegram Definition of Done

Telegram subsystem minimal:

MTProto Connection
Authentication
Session
Reconnect

Update Receiver
Update Normalizer

Telegram API Abstraction

Rate Limiter
Flood Wait Manager

Entity Resolver
Entity Cache

File Transfer
Media Queue
Cancellation
Progress
Cleanup

---

98. Plugin Runtime Definition of Done

Plugin Runtime minimal:

Discovery
Manifest
Validation

Dependency Resolution

Lifecycle
Context
Scope

Capability System
Resource Manager

Storage Namespace
Scheduler Ownership
Worker Ownership
Event Ownership

Plugin Configuration
Plugin API Version
Plugin Migration

Failure Isolation
Panic Recovery

Diagnostics
Leak Detection

---

99. Recommended Development Order

Urutan implementasi:

01 Architecture Principles
02 Dependency Graph
03 Package Boundaries
04 Public Interfaces
05 Error Model
06 Context Model
07 Lifecycle State Machine
08 Runtime Skeleton
09 Resource Ownership Model

10 Event Model
11 EventBus
12 Event Ordering
13 Event Priority
14 Event Middleware
15 Queue
16 Backpressure

17 Worker Model
18 Worker Manager
19 Worker Quota

20 Scheduler
21 Scheduler Persistence
22 Scheduler Recovery

23 Storage
24 Transaction Boundary
25 Migration
26 Plugin Storage Namespace

27 Telegram Abstraction
28 MTProto Adapter
29 Update Receiver
30 Update Normalizer
31 Entity Model
32 Entity Resolver
33 Telegram Events

34 Rate Limiter
35 Flood Wait Manager
36 Idempotency / Deduplication
37 Media/File Transfer

38 Service Registry
39 Core Services
40 Application Use Cases

41 Permission System
42 Command Engine
43 Command Registry
44 Command Middleware
45 Command Conflict Resolution

46 Plugin Runtime
47 Plugin Manifest
48 Plugin Lifecycle
49 Plugin Context
50 Plugin Scope
51 Capability System
52 Resource Manager
53 Plugin Dependency Resolver
54 Plugin Configuration
55 Plugin API Versioning
56 Plugin Diagnostics
57 Plugin Failure Isolation

58 Feature Contract
59 Feature State Model
60 Feature Dependency
61 Feature Conflict
62 Feature Interaction

63 Observability
64 CLI
65 Test Harness
66 Leak Tests
67 Race Tests

68 First Plugin
69 AFK
70 PM Permit
71 Admin
72 Notes
73 Filters
74 Welcome
75 AntiSpam
76 AutoReply
77 Downloader
78 Reminder

---

100. Final Architecture Philosophy

Framework ini harus dipandang sebagai:

«A small operating system for the Telegram userbot.»

Runtime menyediakan:

Lifecycle
Scheduling
Concurrency
Events
Storage
Networking
Resource Management
Capabilities
Observability

Services menyediakan:

Reusable capabilities

Application menyediakan:

Use cases and policies

Feature menyediakan:

Business behavior

Plugin menyediakan:

Lifecycle and packaging boundary

Telegram Boundary menyediakan:

MTProto communication

Prinsip paling penting:

Runtime owns infrastructure.

Services own reusable capabilities.

Application owns use cases.

Features own behavior.

Plugins own feature lifecycle.

Plugins consume capabilities.

Plugins do not own the runtime.

Dan untuk resource:

Every long-lived resource
        ↓
must have an owner
        ↓
must have a cancellation path
        ↓
must have a cleanup path
        ↓
must be observable

Untuk execution:

Scheduler
    = WHEN

Queue
    = PRESSURE / BUFFER

Worker
    = HOW MANY

Task
    = WHAT

Rate Limiter
    = EXTERNAL API CONTROL

Untuk state:

Persistent State
    = survives restart

Runtime State
    = reconstructed

Ephemeral State
    = discarded

Untuk authorization:

Plugin Capability
    ≠
User Permission

Untuk feature:

Feature
    ≠
Infrastructure

Feature
    ≠
Service

Feature
    ≠
Plugin

Feature
    = business/use-case behavior

Plugin
    = lifecycle/package boundary

Dengan boundary tersebut, framework dapat berkembang dari:

simple userbot

menjadi:

full automation runtime

tanpa harus merombak fondasi ketika feature semakin banyak.