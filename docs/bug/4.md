Sudah saya pelajari ulang **Ultroid `main` saat ini**, terutama keseluruhan ekosistem yang berhubungan dengan **Assistant Bot, inline mode, callback, dual-mode command, manager bot, PM bot/relay, custom assistant command, help/menu, deep-link `/start`, dan interactive workflow**. Saya juga mencocokkannya dengan `goultroid:test-next` terkini yang saya verifikasi berada di HEAD **`4a4f2ab19bd3441b8dedc73e20d482a40c464400`** (`fix(media): report deferred synchronous reclamation`, 21 September 2026). [Goultroid test-next](https://github.com/inipew/goultroid/tree/test-next?utm_source=chatgpt.com)

Kesimpulan terpentingnya: **yang harus kita tiru dari Ultroid adalah behavioral contract dan UX-nya, bukan arsitekturnya.** Ultroid sudah menunjukkan dengan sangat jelas apa yang seharusnya dapat dilakukan sebuah assistant bot milik userbot. Goultroid sebaiknya mengimplementasikan kapabilitas itu kembali dari nol dengan model yang lebih typed, scoped, lifecycle-safe, bounded, dan terintegrasi ke execution architecture Goultroid.

### Apa sebenarnya “Assistant Bot” di Ultroid?

Dokumentasi Ultroid sendiri memisahkan tiga primitive utama: `ultroid_cmd` untuk command, `in_pattern` untuk inline assistant update, dan `callback` untuk callback event. `ultroid_cmd` juga menangani mode user/bot/dual/manager. ([GitHub][1])

Tetapi setelah membaca implementasinya, assistant Ultroid sebenarnya jauh lebih besar daripada tiga primitive tersebut. Secara konseptual ia menjalankan **lima peran sekaligus**:

| Peran                             | Ultroid                                                                                              |
| --------------------------------- | ---------------------------------------------------------------------------------------------------- |
| **Owner control plane**           | `/start`, settings, language, timezone, stats, update, addon, PMPermit, bot settings, media settings |
| **Public bot facade**             | welcome, owner info, file/deep-link delivery, help, visitor tracking                                 |
| **Alternative command surface**   | dual-mode commands, manager/group commands, custom `/command`                                        |
| **Interactive UI engine**         | inline query + inline keyboard + callback navigation/workflow                                        |
| **Mailbox / communication proxy** | PM bot relay visitor → owner dan reply owner → visitor                                               |

Jadi kalau tujuan kita hanya membuat `AssistantClient + InlineEngine`, kita masih belum mencapai model Ultroid. **Assistant merupakan surface interaksi lengkap dari userbot.**

Implementasi decorator Ultroid yang menjadi pusat ketiga surface ini ada di `pyUltroid/_misc/_assistant.py`. [Ultroid assistant decorators](https://github.com/TeamUltroid/Ultroid/blob/main/pyUltroid/_misc/_assistant.py?utm_source=chatgpt.com)

---

## Peta fitur Ultroid yang perlu menjadi target Goultroid

Saya akan menganggap tabel berikut sebagai **feature parity target**, bukan spesifikasi implementasi yang harus dicopy.

| Domain                | Perilaku Ultroid                                 | Target Goultroid baru                                   |
| --------------------- | ------------------------------------------------ | ------------------------------------------------------- |
| Assistant runtime     | client bot terpisah dari user account            | `AssistantRuntime` sebagai optional runtime component   |
| Assistant command     | `@asst_cmd`                                      | canonical feature command exposed ke `SurfaceAssistant` |
| Dual mode             | command userbot ikut hidup di assistant          | command yang sama, policy/surface berbeda               |
| Manager bot           | command admin grup melalui bot                   | explicit `AssistantGroup` capability                    |
| Inline                | `@in_pattern`                                    | typed `InlineCapability`                                |
| Callback              | `@callback`                                      | versioned typed action + state token                    |
| `/start`              | public welcome + owner menu                      | `StartFlow` + typed deep-link dispatch                  |
| Settings              | callback + conversation                          | reusable interaction/session workflow                   |
| Help                  | callback menu + inline search                    | generated dari canonical feature metadata               |
| Inline help           | search plugin/command                            | registry-derived, bukan registry kedua                  |
| Saved/custom bot cmd  | `.addcmd` → `/foo`                               | SavedResponse exposed as dynamic assistant command      |
| PM relay              | forward visitor → owner                          | durable `PMRelayService`                                |
| Visitor registry      | `BOT_USERS`                                      | bounded/durable audience registry                       |
| Ban PM user           | `BOTBLS`                                         | typed relay access policy                               |
| Force-sub             | `PMBOT_FSUB`                                     | optional relay admission policy                         |
| Broadcast             | iterate `BOT_USERS`                              | durable bounded fan-out job                             |
| File deep-link        | `/start <key>`                                   | typed expiring `DeepLinkToken`                          |
| Localization          | callback language menu                           | presentation locale context                             |
| Inline utility        | search/download/calc/etc                         | feature-provided inline capability                      |
| Inline workflow       | query → result → callback → edit                 | interaction state machine                               |
| Self-inline rendering | userbot queries own assistant then clicks result | centralized `RenderBridge`, not plugin hack             |
| Media/buttons         | inline-generated button messages                 | canonical render/presentation abstraction               |
| Plugin help           | HELP/LIST global maps                            | generated `FeatureCatalog`                              |
| Onboarding            | callback next/back screens                       | typed navigation flow                                   |

Ultroid’s actual `assistant/` directory juga menunjukkan breadth tersebut: `start.py`, `callbackstuffs.py`, `pmbot.py`, `inlinestuff.py`, `ytdl.py`, `games.py`, localization dan `assistant/manager/*`. Ini bukan sekadar bot `/start`. [Ultroid assistant directory](https://github.com/TeamUltroid/Ultroid/tree/main/assistant?utm_source=chatgpt.com)

---

# Inline Ultroid jauh lebih penting daripada sekadar “inline search”

Ini salah satu temuan paling penting untuk desain Goultroid.

Di Ultroid, inline mempunyai **tiga fungsi berbeda**.

**Pertama, inline sebagai feature surface.** Misalnya search/app lookup, YouTube, calculator, F-Droid, Google, Saavn, TL object dan berbagai utility lainnya. Dekorator resminya memang dimaksudkan sebagai “Inline Assistant Updates”. ([GitHub][1])

**Kedua, inline sebagai interactive application.** Contoh YouTube:

```text
inline query
    ↓
search results
    ↓
choose result
    ↓
callback: audio / video
    ↓
format selection
    ↓
callback
    ↓
download / edit message
```

Calculator juga melakukan pola serupa: inline message menjadi UI, kemudian tombol callback memutasi state/calculation. Implementasi calculator bahkan menggunakan assistant inline query dari user account lalu meng-click result dengan `hide_via=True`. ([CodeFactor][2])

**Ketiga, dan yang paling menarik: inline sebagai rendering bridge.**

`plugins/_inline.py` memiliki mekanisme `something(...)` dan `STUFF`. Secara kasar:

```text
userbot feature
    ↓
ingin mengirim UI/button-rich message
    ↓
simpan temporary presentation
    ↓
query @assistant stf<ID>
    ↓
assistant menghasilkan inline result
    ↓
userbot click inline result ke target chat
    ↓
Telegram menampilkan interactive message
```

Artinya, Ultroid memakai Assistant Bot untuk mengatasi boundary Telegram antara **user account** dan **bot-style interactive markup**.

[Ultroid inline bridge implementation](https://github.com/TeamUltroid/Ultroid/blob/main/plugins/_inline.py?utm_source=chatgpt.com)

### Ini harus kita pertahankan secara semantik

Tetapi **jangan** membuat plugin Goultroid:

```go
assistant.InlineQuery(...)
cache[id] = ...
Click(...)
```

secara manual.

Di desain baru seharusnya:

```text
Feature
  │
  ▼
PresentationService
  │
  ├── direct userbot rendering
  │
  ├── assistant bot rendering
  │
  └── inline render bridge
```

Feature hanya mengatakan:

```go
Render(ctx, View{...})
```

Transport menentukan bagaimana Telegram harus mewujudkannya.

Ini akan membuat inline bridge menjadi **platform capability**, bukan trik yang tersebar di plugin.

---

# `/start` Ultroid juga merupakan router, bukan hanya greeting

`assistant/start.py` melakukan banyak hal sekaligus.

Untuk user biasa, ia memberikan welcome, custom text/media, owner information, optional PM relay, dan menangani `/start <payload>` untuk stored file.

Untuk owner/sudo, `/start` berubah menjadi control console dengan menu Settings, Language, Stats, Broadcast, Timezone, dan submenu lain.

Ia juga menyimpan visitor ke `BOT_USERS` dan optional mencatat visitor baru ke log channel.

[Ultroid assistant start flow](https://github.com/TeamUltroid/Ultroid/blob/main/assistant/start.py?utm_source=chatgpt.com)

Goultroid baru sebaiknya tidak membuat satu handler `/start` raksasa. Modelnya:

```text
/start [payload]
       │
       ▼
DeepLinkRouter
       │
       ├── no payload ───────► HomeFlow
       ├── file token ───────► SavedAssetFlow
       ├── feature token ────► FeatureFlow
       ├── auth token ───────► AuthorizationFlow
       └── expired/invalid ──► Safe fallback
```

Dan `HomeFlow` sendiri memilih presentation berdasarkan principal:

```text
anonymous/public
owner
sudo
authorized assistant user
```

Dengan begitu, nanti MyXL, SavedResponse, plugin configuration, file sharing atau workflow lain dapat memakai `/start` tanpa parsing string sendiri-sendiri.

---

# PM Bot Ultroid layak dianggap subsystem tersendiri

`assistant/pmbot.py` memperlihatkan konsep yang cukup bagus secara produk:

```text
visitor
   │
   │ DM assistant
   ▼
Assistant
   │
   ├── force-sub validation
   ├── blocked-user check
   │
   ▼
forward to owner
   │
   ▼
store forward-message → visitor mapping
   │
owner replies
   │
   ▼
lookup visitor
   │
   ▼
Assistant sends reply
```

Ada juga `/who`, `/ban`, `/unban`.

[Ultroid PM bot relay](https://github.com/TeamUltroid/Ultroid/blob/main/assistant/pmbot.py?utm_source=chatgpt.com)

Di Goultroid jangan memasukkannya ke generic incoming-message handler.

Saya ingin nanti ada:

```text
PMRelayService
 ├─ AdmissionPolicy
 ├─ ConversationRepository
 ├─ RelayMapper
 ├─ BlockRepository
 ├─ DeliveryService
 └─ CleanupPolicy
```

Mapping harus durable dan bounded. Kalau owner membalas forwarded message setelah restart, mapping seharusnya tetap dapat ditemukan selama retention period.

Ultroid menyelesaikan use-case tersebut, tetapi implementasinya masih sangat global/ad-hoc. Kita ambil **semantics-nya**, lalu implement ulang dengan lifecycle yang benar.

---

# Custom Assistant Command juga bukan fitur terpisah dari SavedResponse

Ultroid memiliki `.addcmd`, `.remcmd`, `.listcmd`.

User mereply sebuah message, kemudian:

```text
.addcmd hello
         │
         ▼
save text/media/buttons
         │
         ▼
/hello pada assistant
         │
         ▼
send saved response
```

Implementasinya ada di `plugins/asstcmd.py`. [Ultroid custom assistant commands](https://github.com/TeamUltroid/Ultroid/blob/main/plugins/asstcmd.py?utm_source=chatgpt.com)

Untuk Goultroid, ini tidak perlu dibuat sebagai database/subsystem baru.

Kita sudah punya konsep **SavedResponse** yang jauh lebih advanced. Maka desain yang benar nanti:

```text
SavedResponse
      │
      ├── trigger: userbot command
      ├── trigger: assistant command
      ├── trigger: inline result
      ├── trigger: callback
      └── trigger: deep-link
```

Dengan kata lain, `/foo` di assistant hanya **binding terhadap SavedResponse**, bukan memiliki storage sendiri.

---

# Hal yang tidak boleh kita copy dari Ultroid

Arsitektur Ultroid berhasil secara fungsional, tetapi beberapa mekanismenya justru menjadi contoh apa yang perlu kita hindari.

`asst_cmd`, `in_pattern`, dan `callback` langsung menambahkan event handler ke global `asst`. `ultroid_cmd` juga dapat memasang wrapper yang sama ke userbot maupun assistant ketika dual mode aktif. [Ultroid command decorator internals](https://github.com/TeamUltroid/Ultroid/blob/main/pyUltroid/_misc/_decorators.py?utm_source=chatgpt.com)

Loader kemudian mengaktifkan folder `assistant`, `assistant/manager`, `pmbot`, addons, dan lain-lain secara conditionally. [Ultroid plugin loader](https://github.com/TeamUltroid/Ultroid/blob/main/pyUltroid/startup/loader.py?utm_source=chatgpt.com)

Ini menghasilkan coupling seperti:

```text
plugin
 ├── knows global assistant client
 ├── registers Telegram event itself
 ├── reads global DB key itself
 ├── owns temporary cache itself
 ├── generates callback strings itself
 ├── performs Telegram RPC itself
 └── sometimes owns conversation lifecycle itself
```

**Goultroid baru tidak boleh begitu.**

Plugin/feature tidak boleh tahu dispatcher Telegram, global assistant instance, atau registrasi low-level event.

---

# Kondisi Goultroid sekarang: fondasi kita sebenarnya sudah lebih bagus

Ini penting karena “reimplement dari 0” **tidak berarti membuang execution architecture Goultroid yang sudah benar**.

Di HEAD `4a4f2ab...`, kita sudah punya beberapa konsep yang jauh lebih sehat.

`core.Command` saat ini sudah memiliki:

```text
Permission
InvocationPolicy
Surfaces
Cooldown
Timeout
Resources
Scope
Handler
```

[Current Goultroid command model](https://github.com/inipew/goultroid/blob/test-next/internal/core/command.go?utm_source=chatgpt.com)

Dan `InvocationPolicy` sudah dibedakan per surface:

```text
Userbot
Assistant
```

Itu tepat. Satu feature dapat owner-only di userbot tetapi public pada assistant tanpa mengacaukan `Permission`.

`ExecutionSource` juga sudah mengenal `ExecutionAssistant`, Scheduled, System, Addon dan Automation. Jadi assistant tidak perlu berpura-pura sebagai incoming userbot message.

Assistant command router sekarang juga sudah memakai **canonical `core.Router`** dan secara eksplisit mengatakan presentation command tidak boleh menjadi plugin command registry kedua. Resource-bearing assistant commands masuk ke shared TaskEngine. [Current Goultroid Assistant command router](https://github.com/inipew/goultroid/blob/test-next/internal/assistant/command/router.go?utm_source=chatgpt.com)

Ini harus dipertahankan sebagai invariant baru.

---

# Inline infrastructure Goultroid saat ini juga sudah lebih matang daripada Ultroid

`internal/services/inline/types.go` sekarang sudah punya:

```text
Article
Photo
Document
Video
GIF
Audio
Venue
Geo
Contact
Game
```

serta:

```text
CacheGlobal
CachePerUser
CachePerChat
CacheNone
```

dan response semantics untuk pagination, private results, gallery, SwitchPM serta WebView.

[Current Goultroid inline types](https://github.com/inipew/goultroid/blob/test-next/internal/services/inline/types.go?utm_source=chatgpt.com)

Current inline engine juga sudah melakukan timeout, panic recovery, authorization, cache scoping, pagination, limit 50 result, fallback ketika kosong/error dan protection agar private/callback-rich results tidak bocor melalui global cache.

Jadi masalah kita **bukan kekurangan InlineEngine primitive**.

Masalahnya saat ini product-level feature baru kira-kira:

```text
help
ping
catch-all
```

di `internal/app/inline_handlers.go`.

[Current default inline handlers](https://github.com/inipew/goultroid/blob/test-next/internal/app/inline_handlers.go?utm_source=chatgpt.com)

Jadi situasinya:

```text
Goultroid:
excellent-ish infrastructure
        +
shallow feature integration

Ultroid:
very rich feature integration
        +
much weaker boundaries
```

Target kita adalah **menggabungkan keunggulan keduanya**.

---

# North-star architecture yang saya sarankan

Saya ingin reimplementation baru berpusat pada **Feature Capability**, bukan `assistant/`, `inline/`, dan `callback/` sebagai dunia yang terpisah.

Secara konseptual:

```text
                       ┌─────────────────┐
                       │     Feature     │
                       │   Capability    │
                       └────────┬────────┘
                                │
              ┌─────────────────┼─────────────────┐
              │                 │                 │
         CommandSpec       InlineSpec         UISpec
              │                 │                 │
        ┌─────┴─────┐           │          ┌──────┴──────┐
        │           │           │          │             │
     Userbot    Assistant     Inline     Callback       Menu
        │           │           │          │             │
        └───────────┴─────┬─────┴──────────┴─────────────┘
                          │
                    Interaction Runtime
                          │
              ┌───────────┼────────────┐
              │           │            │
           Policy      Sessions     Presentation
              │           │            │
              └───────────┼────────────┘
                          │
                    Execution Runtime
                          │
              TaskEngine / RPC / Storage
```

Satu feature misalnya `calculator` tidak lagi memiliki “plugin userbot + inline plugin + callback plugin”.

Ia menjadi satu feature:

```go
Feature {
    Commands:  ...
    Inline:    ...
    Actions:   ...
    Screens:   ...
}
```

Runtime yang memasangnya ke surface yang benar.

---

## Feature descriptor harus menjadi sumber kebenaran tunggal

Misalnya secara konseptual:

```go
FeatureSpec{
    ID: "calculator",

    Commands: []CommandSpec{
        {
            Name: "calc",
            Surfaces: Userbot | Assistant,
        },
    },

    Inline: []InlineSpec{
        {
            Pattern: "calc",
            Access: OwnerOrSudo,
        },
    },

    Actions: []ActionSpec{
        {Name: "digit"},
        {Name: "op"},
        {Name: "clear"},
    },

    Screens: []ScreenSpec{
        {ID: "calculator.main"},
    },
}
```

Hasilnya help, inline help, `/start` menu, command registration, unload/reload, metrics attribution dan authorization semuanya membaca metadata yang sama.

Tidak ada lagi:

```text
HELP map
LIST map
InlinePlugin map
AST_PLUGINS map
callback string tersebar
```

seperti pola lama Ultroid.

---

# Callback baru harus lebih kuat daripada milik Ultroid maupun Goultroid sekarang

Current Goultroid sebenarnya sudah bagus karena callback data sudah versioned:

```text
a1:<namespace>:<action>:<state>
```

dengan batas Telegram 64 byte dan field validation. [Current Goultroid callback payload](https://github.com/inipew/goultroid/blob/test-next/internal/assistant/callback/payload.go?utm_source=chatgpt.com)

Namun pada rewrite saya ingin kita naik satu tingkat.

Jangan memasukkan mutable state secara bebas ke callback data.

Target:

```text
a2:<feature>:<action>:<token>
```

`token` menunjuk pada:

```text
InteractionSession
 ├─ feature generation
 ├─ owner/user
 ├─ chat
 ├─ message
 ├─ current screen/state
 ├─ arbitrary bounded state
 ├─ created_at
 ├─ expires_at
 └─ version
```

Saat callback masuk:

```text
parse
  ↓
resolve token
  ↓
validate TTL
  ↓
validate plugin generation
  ↓
validate actor
  ↓
validate chat/message binding
  ↓
policy
  ↓
transition
  ↓
render
```

Ini menyelesaikan sekaligus stale button, cross-user click, plugin reload, callback replay, state terlalu panjang dan temporary global maps.

---

# “Conversation” juga harus direimplement sebagai state machine

Ultroid banyak menggunakan:

```python
async with bot.conversation(user):
    send("enter value")
    value = await get_response()
```

Enak ditulis, tetapi membuat lifetime task menggantung dan logic conversation bercampur dengan transport.

Goultroid sebaiknya:

```text
callback Settings → Set Timezone
          ↓
create InteractionSession
state = AwaitTimezone
          ↓
send prompt
          ↓
handler selesai
          .
          .
next incoming message
          ↓
SessionRouter
          ↓
AwaitTimezone handler
          ↓
validate
          ↓
save typed setting
          ↓
destroy session
          ↓
render Settings screen
```

Keuntungan terbesar: **tidak ada goroutine yang tidur menunggu user selama menit**, shutdown sederhana, restart bisa ditangani, TTL jelas dan resource idle hampir nol.

Ini sangat cocok dengan tujuan Goultroid yang ringan saat idle.

---

# Pisahkan control plane dari public/data plane

Assistant Ultroid mencampur owner settings, public `/start`, PM relay, group manager dan inline utilities dalam satu client/event space.

Di Goultroid satu Telegram bot tetap bisa melayani semuanya, tetapi secara logical harus terpisah:

```text
AssistantRuntime
 │
 ├── ControlPlane
 │    ├── owner settings
 │    ├── status
 │    ├── plugin management
 │    └── diagnostics
 │
 ├── PublicPlane
 │    ├── /start
 │    ├── public commands
 │    └── deep links
 │
 ├── InlinePlane
 │    ├── query
 │    └── inline callbacks
 │
 ├── RelayPlane
 │    └── PM relay
 │
 └── GroupPlane
      └── manager/admin features
```

Masih satu connection/client jika itu paling efisien, tapi policy, quotas, metrics, rate limit dan execution admission dipisah.

---

# Help harus sepenuhnya generated

Ultroid memiliki HELP/LIST maps dan beberapa help UI terpisah.

Pada Goultroid baru:

```text
FeatureCatalog
      ↓
┌───────────────┬────────────────┬────────────────┐
│ userbot help  │ assistant help │ inline help    │
└───────────────┴────────────────┴────────────────┘
```

Command yang:

```text
SurfaceUserbot only
```

tidak muncul sebagai Assistant command.

Command `OwnerOnly` tidak dipresentasikan sebagai public action kepada visitor.

Inline-specific feature otomatis muncul dalam inline discovery.

Ini akan menghilangkan drift yang sekarang sangat mudah terjadi di framework seperti Ultroid.

---

# Area yang saya ingin kita buat dari nol

Karena Anda memang ingin rework/reimplement, saya tidak menyarankan mencoba “memperbaiki sedikit-sedikit” `internal/assistant` sekarang.

Namun boundary reimplementation-nya adalah **assistant interaction stack**, bukan TaskEngine/RPC/core infrastructure seluruh Goultroid.

Urutan implementasi yang menurut saya paling tepat adalah:

1. **P0 — Interaction/Surface Contract**: `FeatureSpec`, capability types, canonical surface registry, ownership/lifecycle, typed policy; belum membuat banyak fitur.
2. **P1 — Interaction Session Runtime**: durable/ephemeral bounded state, TTL, user/chat/message binding, callback `a2`, navigation dan prompt/response state machine.
3. **P2 — Presentation + Render Bridge**: canonical `View`, buttons/actions, edit/send/answer, assistant-render dan self-inline bridge tanpa feature mengetahui Telegram trick.
4. **P3 — Assistant shell**: clean AssistantRuntime, update demux, `/start`, deep-link router, owner/public home, typed menu/navigation, help/status/settings.
5. **P4 — Inline vNext**: registry berasal dari FeatureSpec, search/result/pagination/cache/access policies, feature-provided inline handlers, inline callback continuity.
6. **P5 — SavedResponse integration**: saved response → assistant command / inline / deep-link, menggantikan konsep Ultroid `ASST_CMDS`.
7. **P6 — PM Relay**: durable thread mapping, visitor registry, block/access policy, optional force-sub, owner reply, retention/cleanup.
8. **P7 — group/manager + advanced workflows**: admin feature exposure, calculator, downloader/search workflows, callback-heavy flows sebagai validation.
9. **P8 — migration/parity + delete old stack**: behavioral matrix Ultroid vs Goultroid, resource/idle/load tests, unload/reload/shutdown tests, kemudian hapus implementation lama.

Ini satu-satunya daftar phase yang saya sarankan; fitur lain masuk ke capability framework tersebut, bukan menambah arsitektur baru tiap kali.

---

# P0 harus dimulai dari contract, bukan dari `/start`

Ini bagian yang paling penting untuk langkah selanjutnya.

Saya **tidak akan mulai rewrite dengan membuat `assistant/start.go` baru**.

Kalau kita mulai dari `/start`, besar kemungkinan kita kembali membangun:

```text
/start router
callback router
inline router
settings router
plugin router
```

secara terpisah.

Kita harus mulai dari pertanyaan:

> **“Bagaimana sebuah feature Goultroid menyatakan bahwa dirinya tersedia melalui userbot command, assistant command, inline query, callback action, menu, atau deep-link tanpa mengetahui transport implementation?”**

Kalau contract ini benar, `/start`, inline YouTube, settings, calculator, PM bot dan SavedResponse menjadi konsumen biasa.

---

# Definition of Done akhir

Untuk saya, reimplementation ini baru dapat disebut berhasil bila skenario seperti berikut bekerja:

```text
plugin loaded
   ↓
register FeatureSpec sekali
   ↓
runtime otomatis mengetahui:
  userbot commands
  assistant commands
  inline entries
  callback actions
  menu screens
  help metadata
  invocation policy
  resources
  plugin scope
```

dan ketika plugin unload:

```text
commands hilang
inline handlers hilang
callbacks tidak bisa mengeksekusi generation lama
sessions dibatalkan/expire
resource ownership dilepas
tidak ada dangling handler
tidak ada global cache milik plugin
```

Kemudian sebuah request Assistant apa pun tetap mengikuti:

```text
Telegram update
   ↓
cheap classification
   ↓
surface/feature lookup
   ↓
policy/admission
   ↓
TaskEngine bila memerlukan execution resources
   ↓
feature use-case
   ↓
PresentationService
   ↓
RPC executor
```

**Tidak ada plugin yang langsung mendaftarkan event handler ke Telegram client. Tidak ada plugin yang membuat callback-data protocol sendiri. Tidak ada plugin yang menyimpan conversation goroutine sendiri. Tidak ada command registry kedua khusus Assistant.**

Itulah target yang menurut saya paling tepat.

### Kesimpulan

Setelah membedah Ultroid, saya akan menetapkan tujuan Goultroid seperti ini:

> **Goultroid Assistant vNext harus memiliki kelengkapan UX Ultroid—control bot, dual command surface, rich inline, callbacks, interactive menus, PM relay, custom responses, deep links, manager functionality, render bridge—tetapi semuanya merupakan surface dari feature/use-case Goultroid yang sama, bukan sekumpulan bot plugins independen.**

Yang bagus dari current Goultroid seperti **canonical `core.Command`, `InvocationPolicy`, execution surfaces, TaskEngine, centralized RPC, plugin scope, callback payload validation dan inline result model** tetap menjadi dependency/foundation. Yang kita **reimplement dari nol adalah interaction model dan assistant-facing architecture di atasnya**. Dengan pendekatan itu kita tidak mundur dari redesign yang sudah kita lakukan hanya demi meniru Ultroid.

Langkah pengerjaan yang paling tepat setelah studi ini adalah **P0 — desain dan implement `Feature Surface / Interaction Contract`**, sebelum menyentuh `/start`, settings atau inline feature satu per satu.

[1]: https://github.com/TeamUltroid/Ultroid/wiki/Creating-Plugins?utm_source=chatgpt.com "Creating Plugins · TeamUltroid/Ultroid Wiki · GitHub"
[2]: https://www.codefactor.io/repository/github/teamultroid/ultroid/source/main/plugins/calculator.py?utm_source=chatgpt.com "plugins/calculator.py at main - TeamUltroid/Ultroid"


------------------------------------------------------------------------------------------------------------------------------

Saya sudah refresh ulang konteks P8 berdasarkan **HEAD `test-next` aktual**, roadmap canonical `docs/bug/4.md`, source vNext yang sekarang, dan Ultroid `main` terbaru.

Current HEAD Goultroid:

`0166c952f3cdfd2625d409194f15918fc540e3b5` — `update docs`

Perubahan setelah closure P7 `7c1ce5e1...` hanya `docs/bug/4.md`; implementation P7 yang kita tutup tetap sama. Ultroid `main` yang saya jadikan reference parity saat refresh ini berada di `edd18d31eae982dff468bd8ce785fbb319e370c3`.

Ada satu koreksi konteks penting: **P8 canonical bukan sekadar “advanced workflows”.** `docs/bug/4.md` saat ini mendefinisikan:

> **P8 — migration/parity + delete old stack**: behavioral matrix Ultroid vs Goultroid, resource/idle/load tests, unload/reload/shutdown tests, kemudian hapus implementation lama.

Tetapi roadmap yang sama sebelumnya memasukkan `calculator`, downloader/search, dan callback-heavy workflows ke P7. P7 yang baru kita selesaikan pada praktiknya berkembang menjadi **Group/Manager Plane A–L**, bukan advanced inline workflows. Jadi P8 harus menyerap pekerjaan advanced workflow yang belum terjadi **sebelum** kita bisa melakukan final parity/reclamation. Ini bukan scope tambahan arbitrer; ini konsekuensi dari drift roadmap aktual.

### Kondisi fondasi sekarang

P0–P7 jauh lebih matang daripada saat roadmap awal dibuat. Kita **tidak perlu redesign Assistant lagi**.

Yang sudah ada dan harus diperlakukan sebagai invariant P8:

* `internal/feature`: canonical `FeatureSpec`, interaction kinds `inline/action/screen/deep_link`, explicit policy, lifecycle generation.
* Plugin manager otomatis memproyeksikan semua `core.Command` ke FeatureCatalog; tidak ada command registry Assistant kedua.
* Feature-owned inline binding sudah mengikuti plugin lifecycle dan generation.
* P1 interaction runtime bounded: default 4096 sessions global, 512/plugin generation, 64/actor, 64 KiB/session, 8 MiB retained state; no ticker/goroutine.
* P2 presentation bridge + typed `a2` actions.
* P3 orchestration engine.
* Assistant ingress sudah a2-native.
* Shell `/start`, Help, Status, Settings, free-form settings flow sudah migrated.
* SavedResponse sudah punya Assistant command, inline, callback, dan deep-link surfaces.
* PM Relay P6 sudah durable/bounded.
* MyXL sudah menjadi a2 feature dengan typed screens/action slots/input sessions.
* P7 A–L sudah menutup group/manager plane.
* Legacy Assistant `a1/menu` stack **sudah benar-benar dihapus**, bahkan ada `internal/assistant/legacy_stack_test.go` yang melarang `LegacyAssistantMenu`, `CompatibilityHost`, `MenuInstanceStore`, `a1:` dan package legacy kembali masuk production.

Jadi bagian “delete old stack” P8 sudah sebagian besar dikerjakan lebih awal oleh P6 cutover. P8 tidak boleh menghapus compatibility code secara membabi-buta hanya karena ada nama `compat`.

Contohnya `savedresponse/registry_compat.go` dan `ledger_compat.go` adalah compatibility untuk **media ownership migration**, bukan Assistant a1. Itu bukan kandidat delete otomatis.

### Gap terbesar P8 sekarang: advanced product parity

Current production inline shell yang saya verifikasi menyediakan:

```text
root
help
ping
```

dan ada dynamic SavedResponse inline source.

Infrastructure inline sendiri sudah sangat kuat: result types, access/cache policies, pagination foundation, lifecycle-scoped handler registration, TaskEngine admission, local media handling, dll.

Masalahnya sekarang adalah:

```text
strong inline infrastructure
+
very small advanced inline feature set
```

Ini persis kebalikan Ultroid.

Ultroid `main` saat ini masih mempunyai advanced interaction seperti:

* calculator: command → self-inline render → callback state;
* YouTube: query → result → audio/video → format → download;
* Google search;
* application lookup;
* F-Droid;
* OrangeFox;
* Saavn;
* TL object lookup;
* code execution/Piston;
* dan beberapa utility lainnya.

Saya **tidak menyarankan clone semuanya satu-per-satu**. Behavioral parity harus menguji kemampuan platform, bukan jumlah integration provider.

### Self-inline / render bridge masih belum production-complete

Ini gap arsitektural P8 yang menurut saya paling penting sebelum calculator/downloader.

P2 punya:

```text
presentation.View
presentation.Port
presentation.Compiler
telegram.Bridge
```

dan `PluginManager.NewInteractionEngine(port)` memang didesain agar Assistant dan userbot-inline bisa berbagi semantics.

Tetapi production `internal/presentation/telegram/bridge.go` saat ini:

```text
Send() -> MessageTarget only
Edit() -> MessageTarget atau InlineTarget
```

Saya belum menemukan production **userbot → query own Assistant → click inline result** adapter seperti semantic Ultroid `something(...)` / self-inline rendering.

Jadi kontraknya ada, tetapi **RenderBridge self-inline parity belum benar-benar selesai**.

Itu harus diselesaikan sebelum calculator, karena kalau calculator langsung membuat inline query/click sendiri, kita kembali ke arsitektur Ultroid yang ingin kita hindari.

---

## Proposed P8 baru

Saya menyarankan P8 dipecah seperti ini.

### P8-A — current-head parity inventory + target freeze

Ini harus pertama.

Buat matrix dari **behavior**, bukan nama file Ultroid:

```text
Behavior
Ultroid surface
Goultroid equivalent
Current status
Required parity
Resource profile
Lifecycle owner
Acceptance test
```

Domain matrix minimal:

```text
Assistant shell/control
public /start
dual command
help/catalog
settings/input
SavedResponse
deep-link
PM relay
audience/broadcast
group/manager
inline discovery
callback workflow
self-inline rendering
calculator
search/lookup
interactive downloader
plugin reload
shutdown
idle/load
```

Output P8-A harus menentukan dengan tegas:

```text
Must parity
Representative parity
Out of scope
```

Supaya P8 tidak berkembang menjadi “implement semua plugin Ultroid”.

### P8-B — production self-inline / RenderBridge

Tujuan:

```text
Feature
  ↓
presentation.View
  ↓
RenderBridge
  ├─ normal userbot render
  ├─ Assistant message render
  └─ self-inline render
```

Feature tidak boleh:

```go
assistant.InlineQuery(...)
results[0].Click(...)
```

sendiri.

Boundary yang harus dipertahankan:

* a2 session/action semantics sama;
* lifecycle scope sama;
* stale generation fail-closed;
* no temporary global `STUFF` map;
* bounded retained presentation;
* cancellation mengikuti TaskEngine/plugin scope;
* Assistant transport restart tidak membuat feature state orphan.

Ini adalah missing platform capability yang nyata.

### P8-C — calculator as callback-heavy canary

Calculator sangat ideal sebagai first advanced workflow karena tidak mempunyai network/download complexity.

Ultroid menggunakan global:

```python
CALC = {}
```

Goultroid jangan.

Target:

```text
.calc / inline calc
        ↓
FeatureSpec
        ↓
P1 session state
        ↓
a2 button actions
        ↓
bounded expression state
        ↓
render/update
```

Syarat:

* no `eval`;
* typed/safe expression parser;
* state bound per session;
* stale buttons rejected;
* cross-user click rejected;
* plugin disable cancels session;
* reload generation invalidates old calculator;
* userbot command dapat menggunakan RenderBridge yang sama;
* inline invocation dan userbot invocation tidak membuat calculator engine kedua.

Calculator ini akan menjadi proof bahwa P0–P3 architecture benar-benar general, bukan hanya Settings/MyXL.

### P8-D — search/lookup inline workflow

Kita sebenarnya sudah punya `plugins/wikipedia` dan command itu sudah:

```go
Surfaces: Userbot | Assistant
```

Jadi saya tidak akan membuat search subsystem baru.

P8-D sebaiknya:

* tambah FeatureSpec inline binding ke use-case search yang sudah ada;
* gunakan network capability existing;
* bounded result count;
* pagination/cache policy eksplisit;
* SwitchInline/share behavior bila memang dibutuhkan;
* no process-local unbounded result cache;
* cancellation dari inline task;
* no one-handler-per-query state.

Wikipedia dapat menjadi **first low-risk network inline canary**.

Setelah itu bila ingin behavioral coverage lebih luas, kita bisa tambah satu general web/app lookup. Tidak perlu meng-copy OrangeFox + F-Droid + Twitter + Saavn hanya untuk membuktikan runtime.

### P8-E — interactive downloader / YouTube workflow

Ini workload P8 paling berat.

Current Goultroid `plugins/downloader` sebenarnya sudah bagus sebagai backend:

* TaskEngine continuation;
* `download` resource;
* extractor menambah `process`;
* direct HTTP tidak memegang `process`;
* timeout 10 menit;
* 500 MiB bound;
* scoped temp workspace;
* retained media ownership;
* no background worker sendiri.

Tetapi command sekarang default **userbot-only** karena tidak mempunyai `Surfaces`.

Jangan menyalin `assistant/ytdl.py`.

Target P8:

```text
inline yt <query>
    ↓
bounded search results
    ↓
select result
    ↓
a2 Audio / Video
    ↓
format list
    ↓
select format
    ↓
TaskEngine
    ├─ network/download
    ├─ process only when required
    └─ media/upload
    ↓
edit/deliver result
```

Downloader backend existing tetap satu.

Tidak boleh membuat:

```text
InlineDownloader
AssistantDownloader
UserbotDownloader
```

sebagai tiga engines.

Ini juga harus menguji callback yang hidup lebih lama, cancellation, progress, FloodWait, large media, process budget, dan cleanup.

### P8-F — full behavioral parity matrix

Setelah B–E, baru lakukan matrix Ultroid vs Goultroid secara keseluruhan.

Expected state kira-kira:

| Domain                     | Current                                               |
| -------------------------- | ----------------------------------------------------- |
| `/start` owner/public      | already conform                                       |
| Settings                   | conform                                               |
| Help/catalog               | conform/generated                                     |
| Saved assistant command    | conform                                               |
| inline SavedResponse       | conform                                               |
| deep-link                  | conform                                               |
| typed callback             | conform                                               |
| PM relay                   | conform                                               |
| visitor registry/broadcast | conform                                               |
| force-sub                  | conform                                               |
| group manager              | conform after P7                                      |
| reply/topic/media          | conform after P7                                      |
| MyXL workflow              | conform a2                                            |
| calculator                 | **missing**                                           |
| rich search inline         | **partial**                                           |
| YouTube/download workflow  | **missing interactive surface**                       |
| self-inline rendering      | **contract exists, production capability incomplete** |

Ini menurut saya adalah real parity state sekarang.

### P8-G — compatibility/reclamation audit

P8 original roadmap mengatakan “delete old stack”, tetapi **Assistant a1 stack sudah gone**.

Jadi P8-G harus reference-driven.

Candidates harus dibagi:

```text
DEAD assistant migration code
    → delete

historical architecture regression tests
    → keep

generic plugin callback infrastructure still used
    → keep

raw MessageHookPlugin compatibility
    → audit separately, do not delete just because legacy

SavedResponse media registry compat
    → not Assistant compatibility; retain until media migration exit criteria
```

Yang sangat penting: `internal/assistant/legacy_stack_test.go` justru **harus dipertahankan**, karena itu fence agar a1 tidak masuk lagi.

### P8-H — unload/reload/generation acceptance

Definition of Done original `docs/bug/4.md` harus dibuktikan:

```text
plugin loaded
↓
FeatureSpec visible
↓
command + inline + action + screen visible

plugin disable
↓
commands removed
inline removed
actions removed
sessions canceled
TaskEngine scope canceled
old callback stale
no retained feature cache

plugin enable
↓
new generation
old tokens still invalid
new surfaces work
```

Plugin manager sekarang sudah punya banyak mekanisme ini; P8 tinggal membuat **cross-surface acceptance matrix**, bukan runtime baru.

### P8-I — resource / idle / high-load acceptance

Workloads P8 berbeda dari P7.

Yang perlu diuji:

```text
10k inline queries across many users
callback bursts
session capacity
pagination/cache cardinality
calculator button spam
network search timeout
downloader concurrency
download + process contention
500 MiB media case
plugin disable with active workflow
Assistant restart mid-session
shutdown during extraction/upload
```

Metrics:

```text
goroutine baseline/peak/settled
heap baseline/plateau
RSS baseline/plateau
TaskEngine queue/rejection
session count/bytes
inline cache count/bytes
RPC attempts
download/process grants
shutdown duration
```

Idle expectation tetap:

```text
no active feature
→ no calculator worker
→ no search worker
→ no downloader worker
→ no session cleanup ticker
→ no per-user goroutine
```

### P8-J — final reclamation + closure

Baru terakhir:

* delete proven-dead transitional files/interfaces;
* remove stale compatibility comments;
* update feature parity documentation;
* architecture test “forbidden legacy patterns”;
* freeze final behavioral matrix;
* benchmark/report;
* closure doc.

---

## Hal yang tidak saya sarankan dilakukan di P8

Jangan mulai dengan menghapus compatibility files.

Jangan mulai dengan YouTube.

Jangan membuat `assistant/calculator`, `assistant/search`, atau `assistant/ytdl` package hanya karena Ultroid punya struktur itu.

Jangan memberi downloader Assistant surface dengan sekadar:

```go
Surfaces: Userbot | Assistant
```

karena parity yang dibutuhkan bukan hanya command `/download`; yang belum ada justru interactive inline flow.

Dan jangan membuat global maps seperti Ultroid:

```text
CALC
BACK_BUTTON
APP_CACHE
RECENTS
FDROID_
_savn_cache
```

P1 session runtime dan bounded caches sudah ada untuk menggantikannya.

## Urutan yang saya anggap paling aman

```text
P8-A  parity inventory + target freeze
  ↓
P8-B  production self-inline / RenderBridge
  ↓
P8-C  calculator canary
  ↓
P8-D  search/lookup inline
  ↓
P8-E  interactive downloader / YouTube
  ↓
P8-F  complete behavioral parity matrix
  ↓
P8-G  compatibility/dead-stack reclamation
  ↓
P8-H  unload/reload/generation acceptance
  ↓
P8-I  resource/idle/load acceptance
  ↓
P8-J  final cleanup + closure
```

Jadi rekomendasi saya untuk pengerjaan berikutnya adalah **mulai P8-A**, bukan langsung calculator dan bukan langsung delete old stack.

P8-A harus menghasilkan snapshot canonical “Ultroid behavior → Goultroid behavior → status/gap” terhadap HEAD `0166c952...`, karena dari audit sekarang jelas bahwa migration architecture hampir selesai tetapi **advanced interaction parity masih menjadi gap utama yang diwariskan dari P7 lama**.


=============================================================================================================================

## P8-A CLOSED — behavioral inventory + target freeze

P8-A has been completed against Goultroid `test-next` baseline `8a43ebd8b43a5445d0e688a87b3d3688ec6de80a` and Ultroid `main` baseline `edd18d31eae982dff468bd8ce785fbb319e370c3`.

Canonical inventory:

`docs/design/assistant-parity-p8a-inventory.md`

P8 scope is now frozen into:

- **MUST** — required before P8 closure;
- **REPRESENTATIVE** — common capability must have at least one production proof, but provider-for-provider Ultroid cloning is not required;
- **BASELINE-CLOSED** — already accepted by P0→P7 and not to be redesigned without a concrete defect;
- **EXCLUDED** — explicitly not a P8 parity blocker.

### Remaining MUST items

```text
P8-B  production self-inline / RenderBridge
P8-C  callback-heavy calculator canary
P8-F  locale-aware Assistant presentation
P8-H  unload/reload/generation cross-surface acceptance
P8-I  combined resource/idle/high-load acceptance
```

### Representative capability proofs

```text
P8-D  one production rich network lookup inline workflow
P8-E  one production interactive downloader / YouTube-style heavy workflow
```

Provider-specific Ultroid inline integrations such as Google, F-Droid, OrangeFox, Twitter/X, Saavn, Play Store, TL search, games, and novelty utilities are not individual P8 blockers once the shared interaction capability is proven.

Piston-style remote arbitrary code execution is explicitly excluded from P8 parity.

### Baseline already closed

P8-A confirms that the following do not need another architecture rewrite:

- FeatureSpec/canonical command projection;
- bounded P1 interaction sessions;
- a2 callbacks;
- presentation/orchestration foundation;
- Assistant shell /start, Help, Status, Settings, and free-form input;
- SavedResponse Assistant/inline/callback/deep-link bindings;
- PM Relay, audience registry, block policy, force-sub, and bounded broadcast;
- MyXL a2 workflow;
- P7 Group/Manager Plane A→L;
- removal of the old Assistant a1/menu stack.

`internal/assistant/legacy_stack_test.go` remains a required architecture fence and must not be deleted during P8-G.

SavedResponse media `*_compat.go` files are **not** Assistant legacy code; they belong to media/storage migration and require separate exit criteria.

### Frozen P8 sequence

```text
P8-A  behavioral inventory + target freeze                 CLOSED
P8-B  production self-inline / RenderBridge
P8-C  calculator callback-heavy canary
P8-D  representative rich search/lookup inline
P8-E  representative interactive downloader workflow
P8-F  complete behavioral matrix + remaining MUST UX
      residuals, including locale-aware Assistant UI
P8-G  reference-driven compatibility/dead-stack reclamation
P8-H  unload/reload/generation cross-surface acceptance
P8-I  resource/idle/high-load acceptance
P8-J  final cleanup, parity freeze, and closure
```

Next work: **P8-B — production self-inline / RenderBridge**.


=============================================================================================================================

## P8-B CLOSED — production self-inline / RenderBridge

P8-B adds the production userbot → own Assistant inline rendering boundary required by callback-heavy advanced features.

Canonical design:

`docs/design/assistant-parity-p8b-self-inline-renderbridge.md`

Implemented boundaries:

```text
feature
  ↓
selfinline.Renderer
  ↓
stateless RenderBridge
  ↓
telegram.Service
  ↓
shared Resolver + RPCExecutor
  ↓
messages.getInlineBotResults
messages.sendInlineBotResult
```

Key properties:

- no raw Telegram client is exposed to feature code;
- no second callback/session runtime;
- no global result/session map;
- no worker/goroutine/ticker;
- current Assistant username is resolved lazily on every render;
- query/offset/result selection are bounded;
- exactly one inline result is inserted;
- Telegram random_id uses crypto/rand;
- reply/topic context is preserved;
- Telegram calls remain behind the canonical RPC executor/limiter;
- existing P1 first-callback inline target claim preserves a2 continuity without a new mapping table.

Source acceptance includes unit coverage for selection/fail-closed/topic behavior and architecture fences for single managed transport + zero retained bridge runtime.

P8-B changes were formatted with `gofmt` before commit.

Next: **P8-C — calculator callback-heavy canary**.


=============================================================================================================================

## P8-C CLOSED — calculator callback-heavy canary

P8-C now provides the first production advanced feature consuming P8-B self-inline rendering and the existing P1/a2 interaction runtime.

Canonical design:

`docs/design/assistant-parity-p8c-calculator-canary.md`

Production flow:

```text
.calc
  ↓
P8-B selfinline.Renderer
  ↓
own Assistant inline result
  ↓
FeatureSpec-owned calculator inline surface
  ↓
bounded P1 expression state
  ↓
typed a2 buttons
  ↓
Assistant callback TaskEngine path
  ↓
session Transition / inline edit
```

Key closure properties:

- calculator is a normal generated module/plugin;
- command remains canonical in `core.Router`;
- one inline interaction + fixed typed action identities;
- no raw callback bytes from calculator feature code;
- no calculator-owned session map;
- no worker/goroutine/ticker;
- expression state capped at 128 bytes;
- arithmetic uses a dedicated bounded recursive-descent evaluator, not eval/process execution;
- per-button transition advances session revision;
- stale old buttons and wrong actors are rejected by existing P1 semantics;
- `InlineTarget` action admission revalidates as `SourceInline`;
- self-inline renderer capability is checked on every render through `CapabilityGate`;
- plugin disable/reload cleanup already unregisters old action scope and cancels old session scope.

P8-C includes a real orchestration acceptance using the shared Registry + Runtime + Dispatcher + Engine, proving result transition, stale-token rejection, and cross-user binding rejection.

All changed Go source is formatted with `gofmt` before the P8-C commit.

No CI status was inspected as part of this phase.

Next: **P8-D — representative rich search/lookup inline**.


### P8-C final parser-depth hardening

A final source re-audit after the calculator canary landed added an explicit recursion-depth bound of 32 to the safe evaluator. The limit covers nested parentheses, unary operators, and exponent recursion in addition to the existing 128-byte expression bound.

Regression: `TestEvaluateExpressionDepthBound`.

P8-C remains CLOSED.


### P8-C capability-boundary hardening

Final re-audit of P8-C found one capability-model residual in the generic self-inline injection.

A self-inline render performs both:

```text
messages.getInlineBotResults   -> telegram.read
messages.sendInlineBotResult   -> telegram.send_message
```

The renderer wrapper previously revalidated only `telegram.send_message`.

P8-C now requires both `telegram.read` and `telegram.send_message` on every render, and the calculator module manifest declares both capabilities. No capability result is cached.

The P8-C architecture test fences both checks and both manifest declarations.

P8-C remains **CLOSED** after this hardening.


=============================================================================================================================

## P8-D CLOSED — representative rich search/lookup inline

P8-D fulfills the representative rich network lookup requirement from P8-A using the existing Wikipedia backend instead of creating a second search subsystem.

Canonical design:

`docs/design/assistant-parity-p8d-rich-lookup-inline.md`

Production behavior:

```text
inline: wiki <query>
  ↓
FeatureSpec-owned public inline interaction
  ↓
generation-owned InlineBindings
  ↓
Inline vNext bounded execution
  ↓
Wikipedia searchPages()
  ↓
CapHTTP network.Client
  ↓
one REST search request
  ↓
<= 5 rich article results
```

Key closure properties:

- command and inline surfaces share the same `searchPages` backend;
- inline query is capped at 256 bytes;
- network/result cardinality is capped at 5;
- one inline query performs one search request, not N summary requests;
- rich results include title, description, sanitized excerpt, article URL, and HTTPS thumbnail when available;
- explicit `CacheGlobal` with 60-second Telegram cache time;
- cache ownership remains in Inline vNext and is generation-versioned;
- Wikipedia owns no result map, worker, goroutine, ticker, or query registration;
- inline lifecycle remains `RegisterOwned` / generation-safe cleanup;
- request cancellation flows through the existing inline handler context into `network.Client`;
- the old `network.NewService(nil, nil)` fallback was removed so missing `CapHTTP` fails closed;
- the module remains scoped to `plugin.CapHTTP` only;
- the representative provider requirement is satisfied without making Google/F-Droid/OrangeFox/Saavn/etc. individual P8 blockers.

P8-D source and tests were formatted with `gofmt` before commit.

No CI status was inspected as part of this phase.

Next: **P8-E — representative interactive downloader workflow**.
