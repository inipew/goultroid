# Assistant vNext P6-A — legacy a1 compatibility quarantine

> **Superseded.** This document described the temporary P6-A state while MyXL and the Assistant shell still depended on a1 compatibility infrastructure.
>
> The a1/menu stack was subsequently migrated and removed. The current architecture is fenced by `internal/assistant/legacy_stack_test.go`; P8-G performs the final reference-driven compatibility cleanup. See `docs/design/assistant-parity-p8g-reclamation.md` for the current state.
>
> Historical implementation details remain available in Git history and must not be treated as current production guidance.
